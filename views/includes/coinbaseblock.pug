.well(id='block'+block.block+'chain'+block.chain+'well')
  form.form-horizontal

    .form-group
      label.col-sm-2.control-label(for='block'+block.block+'chain'+block.chain+'number') #{__('Block')}:
      .col-sm-10
        .input-group
          span.input-group-addon #
          input.form-control(id='block'+block.block+'chain'+block.chain+'number', type='number', value=block.block)

    .form-group
      label.col-sm-2.control-label(for='block'+block.block+'chain'+block.chain+'nonce') #{__('Nonce')}:
      .col-sm-10
         input.form-control(id='block'+block.block+'chain'+block.chain+'nonce', type='text', value=block.nonce)

    .form-group
      label.col-sm-2.control-label #{__('Coinbase')}:
      .col-sm-10
        .input-group
          .input-group-addon #{__('$')}
          input.form-control(id='block'+block.block+'chain'+block.chain+'coinbasevalue', type='text', value=block.coinbasevalue)
          .input-group-addon -&gt;
          input.form-control(id='block'+block.block+'chain'+block.chain+'coinbaseto', type='text', value=block.coinbaseto)

    .form-group
      label.col-sm-2.control-label #{__('Tx')}:
      .col-sm-10
        each tx, x in block.txs
          .input-group
            .input-group-addon #{__('$')}
            input.form-control(id='block'+block.block+'chain'+block.chain+'tx'+x+'value', type='text', value=tx.value)
            .input-group-addon #{__('From')}:
            input.form-control(id='block'+block.block+'chain'+block.chain+'tx'+x+'from', type='text', value=tx.from)
            .input-group-addon -&gt;
            input.form-control(id='block'+block.block+'chain'+block.chain+'tx'+x+'to', type='text', value=tx.to)

    .form-group
      label.col-sm-2.control-label.merkle-label(for='block'+block.block+'chain'+block.chain+'merkle') Merkle:
      .col-sm-10
        input.form-control.merkle-field(id='block'+block.block+'chain'+block.chain+'merkle', type='text', disabled, placeholder='hash of all transactions...')

    #state.form-group
      label.col-sm-2.control-label(for='block'+block.block+'chain'+block.chain+'previous') #{__('Prev')}:
      .col-sm-10
        input.form-control(id='block'+block.block+'chain'+block.chain+'previous', type='text', value=block.previous)

    #state.form-group
      label.col-sm-2.control-label(for='block'+block.block+'chain'+block.chain+'hash') #{__('Hash')}:
      .col-sm-10
        input.form-control(id='block'+block.block+'chain'+block.chain+'hash', type='text' disabled)

    .form-group
      .col-sm-10.col-sm-offset-2
        .coinbase-balance-warning(id='block'+block.block+'chain'+block.chain+'balanceWarning')
          span.glyphicon.glyphicon-exclamation-sign
          strong  Fondos insuficientes
          |  — las transacciones superan el coinbase. El minero rechaza este bloque.

    .form-group
      .col-sm-2
        i.icon-spinner.icon-spin.icon-large
      .col-sm-10
        button.btn.btn-primary.ladda-button(id='block'+block.block+'chain'+block.chain+'mineButton', data-style='expand-right')
          span.ladda-label #{__('Mine')}
